# gRPC Desktop App (Electron + React + Python)

## 🧩 Stack
- Electron (native shell)
- React + Vite (frontend)
- Python gRPC server (LangGraph-compatible)

## 🚀 Run Locally

1. Build frontend

```bash
cd frontend
npm install
npm run build
